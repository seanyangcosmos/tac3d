Omega Infinity is built to keep topology upstream from language.

The semantic core coordinates structure, the decision layer arbitrates evidence topology, and the overlay interprets only after that structure is resolved.

That ordering is not presentation. It is a control against narrative outrunning the stack.
