A topology stack should preserve role separation across layers.

The semantic core coordinates evidence structure, the decision layer arbitrates cross-layer alignment, and the overlay layer translates the result into readable form. When those roles blur, interpretation starts to overwrite unresolved structure.
