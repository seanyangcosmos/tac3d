A topology stack remains trustworthy only when each layer stays inside its role.

The semantic core should coordinate structure, the decision layer should arbitrate cross-layer evidence, and the overlay should interpret only after that structure is settled. When those functions blur, unresolved boundaries start to disappear under readable language.
