Topology-aware reasoning depends on ordered layer function.

The semantic core should coordinate structure, the decision layer should arbitrate alignment, and the overlay should interpret only after those earlier steps settle. When interpretation moves upstream, unresolved evidence boundaries become harder to see.
